class Solution:
    def averageValue(self, nums: List[int]) -> int:
        final = [int(num) for num in nums if num % 2 == 0 and num % 3 == 0]
        if len(final) > 0:
            return sum(final)//len(final)
        else:
            return 0
